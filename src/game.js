export class Game {
  constructor (document, context, width, height) {
    this.document = document;
    this.context = context;
    this.width = width;
    this.height = height;
  }

  init () {

  }
}